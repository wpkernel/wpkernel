[**@wpkernel/test-utils v0.12.5-beta.0**](../README.md)

---

[@wpkernel/test-utils](../README.md) / createWordPressTestHarness

# Function: createWordPressTestHarness()

```ts
function createWordPressTestHarness(overrides): WordPressTestHarness;
```

Creates a WordPress test harness with mocked globals.

## Parameters

### overrides

[`WordPressHarnessOverrides`](../interfaces/WordPressHarnessOverrides.md) = `{}`

Optional overrides for WordPress globals.

## Returns

[`WordPressTestHarness`](../interfaces/WordPressTestHarness.md)

A `WordPressTestHarness` instance.
